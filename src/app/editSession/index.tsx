import { View } from "react-native";
import styles from "./styles.module.scss";

export const EditSession: React.FC = (): React.ReactElement => {
    return (
        <View className={styles["container"]}>
            
        </View>
    )
}